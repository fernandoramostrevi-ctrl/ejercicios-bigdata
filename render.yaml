services:
  - type: web
    name: dashboard-asia-central
    env: python
    rootDir: trabajo_final
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run dashboard.py --server.port $PORT --server.address 0.0.0.0
    plan: free
